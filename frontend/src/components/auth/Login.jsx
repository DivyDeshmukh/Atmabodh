import React from 'react'

function Login() {
  return (
    <div>Login</div>
  )
}

export default Login

// build checker whether user is logged in or not, if not then go to login while clicked on any link and if yes then go to that links page.
